import React from 'react';
import { registerUser } from "../api";
import { storeToken } from "../auth";

const Register = (props)=>{
return(
    <div>
        <h2>register goes here</h2>
    </div>
)
}

export default Register;